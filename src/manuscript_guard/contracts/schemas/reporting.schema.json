{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://manuscript-guard.org/schemas/reporting/1",
  "title": "manuscript-guard reporting checklist",
  "description": "A reporting guideline's item list, as published. These are retrieved from the guideline's own site and recorded with their source, never written from memory: a checklist whose items are approximately right is worse than no checklist, because it produces confident coverage of the wrong things.",
  "type": "object",
  "required": ["schema", "name", "source_url", "retrieved_on", "items"],
  "additionalProperties": false,
  "properties": {
    "schema": { "const": "manuscript-guard/reporting/1" },
    "name": { "type": "string", "minLength": 1 },
    "long_name": { "type": "string" },
    "version": { "type": "string" },
    "source_url": { "type": "string" },
    "source_file": {
      "type": "string",
      "description": "The official document the items were transcribed from, stored under profiles/reporting/sources/. Present so a reader can check the transcription against the original, exactly as a literature quote can be checked against its source."
    },
    "retrieved_on": { "type": "string", "format": "date" },
    "retrieved_by": { "type": "string" },
    "applies_to": {
      "type": "string",
      "description": "The study designs this checklist covers, in the guideline's own words."
    },
    "licence": { "type": "string" },
    "verification": {
      "type": "string",
      "description": "How thoroughly the transcription was checked against the source. Recorded because it differs: a Word table allows every item's full text to be verified, while a column-laid-out PDF allows only each item's opening clause, and the two should not be mistaken for the same guarantee."
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "topic", "text"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "description": "The guideline's own item number, e.g. 6a." },
          "section": { "type": "string", "description": "The heading it sits under." },
          "topic": { "type": "string" },
          "text": { "type": "string", "minLength": 1 },
          "applies_to": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Study designs this item is restricted to, where the checklist says so (cohort, case-control, cross-sectional)."
          }
        }
      }
    }
  }
}
